upstream django_app {
    server web:8000;
}

server {
    listen 80;
    server_name localhost 127.0.0.1;
    client_max_body_size 20M;

    # Перенаправляем все трафик на HTTPS в продакшене
    # Раскомментируй эту строку и закомментируй location блоки ниже для продакшена:
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://django_app;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;
    }

    location /static/ {
        alias /app/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location /media/ {
        alias /app/media/;
        expires 7d;
        add_header Cache-Control "public";
    }
}

# HTTPS конфигурация для продакшена (раскомментируй и настрой пути к SSL сертификатам)
# server {
#     listen 443 ssl http2;
#     server_name nerzhaveyka.kg www.nerzhaveyka.kg;
#     client_max_body_size 20M;
#
#     ssl_certificate /etc/letsencrypt/live/nerzhaveyka.kg/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/nerzhaveyka.kg/privkey.pem;
#
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     location / {
#         proxy_pass http://django_app;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_redirect off;
#     }
#
#     location /static/ {
#         alias /app/staticfiles/;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location /media/ {
#         alias /app/media/;
#         expires 7d;
#         add_header Cache-Control "public";
#     }
# }
#
# # Перенаправляем HTTP на HTTPS
# server {
#     listen 80;
#     server_name nerzhaveyka.kg www.nerzhaveyka.kg;
#     return 301 https://$server_name$request_uri;
# }
